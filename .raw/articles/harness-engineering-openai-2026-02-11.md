---
source_url: https://openai.com/zh-Hans-CN/index/harness-engineering/
fetched: 2026-05-11
author: Ryan Lopopolo
published: 2026-02-11
---

# 工程技术：在智能体优先的世界中利用 Codex

作者：Ryan Lopopolo，技术人员
发布日期：2026年2月11日

## 目录

- 我们从一个空的 Git 代码仓库开始
- 重新定义工程师的角色
- 提高应用程序的可读性
- 我们将代码仓库设为记录系统
- 目标是智能体的可读性
- 规范架构与品味
- 吞吐量改变了合并的理念
- "智能体生成"实际上意味着什么
- 不断提高的自主水平
- 熵与垃圾收集
- 我们仍在学习的内容

## 正文

在过去五个月里，我们的团队一直在进行一项实验：构建并交付一款软件产品的内部 beta 版，其中没有一行代码是人工编写的。该产品有内部日常活跃用户和外部 Alpha 测试者。它经历了交付、部署、故障和修复的整个过程。与众不同的是，每一行代码——从应用逻辑、测试、CI 配置、文档、可观察性到内部工具——全都是由 Codex 编写的。据估计，我们只用了手工编写代码所需的大约 1/10 的时间就完成了这项工作。

人类掌舵。智能体执行。

我们有意选择这一限制，以便构建必要的内容，从而将工程速度提升数个数量级。我们用了几周的时间来交付最终达到一百万行代码的项目。

### 我们从一个空的 Git 代码仓库开始

首次提交到一个空的代码仓库是在 2025 年 8 月下旬。初始架构——包括代码仓库结构、CI 配置、格式化规则、包管理器设置和应用框架——是在一小套现有模板的指导下，由 Codex CLI 使用 GPT-5 生成的。就连指导智能体如何在代码仓库中工作的初始 AGENTS.md 文件本身也是由 Codex 编写的。

五个月后，该代码仓库已经拥有约一百万行代码。在那段时间内，大约有 1,500 个 Pull Request 被打开与合并，而推动 Codex 的仅仅是一个由三名工程师组成的小团队。这相当于平均每位工程师每天处理 3.5 个 PRs 的吞吐量，而且令人惊讶的是，随着团队规模扩大到现在的七名工程师，吞吐量甚至还增加了。

在整个开发过程中，人类从未直接贡献过任何代码。这成为团队的核心理念：不手动编写代码。

### 重新定义工程师的角色

由于缺乏人工编码的实践，工程师工作的重点转向了系统、架构和杠杆作用。

早期进展比预期要慢，不是因为 Codex 不具备相应的能力，而是因为环境的规范不够明确。工程团队的主要任务成了协助智能体完成有用的工作——将更大的目标拆解为更小的构建模块，提示智能体去构建，并使用它们去解锁更复杂的任务。

人类几乎完全通过提示与系统交互：工程师描述任务，运行智能体，并允许其打开一个 Pull Request。Codex 直接使用标准开发工具（gh、本地脚本和嵌入代码仓库的技能）来收集情境。

人类可以审核 Pull Request，但并非必须这样做。随着时间的推移，几乎所有的审核工作都调整为智能体对智能体的方式处理。

### 提高应用程序的可读性

随着代码吞吐量的增加，瓶颈变成了人工 QA 能力。由于人类的时间和注意力是固定的限制因素，团队努力通过令应用程序的 UI、日志和应用指标等内容对 Codex 直接可读，从而为智能体增加更多功能。

- 应用程序可以根据 git worktree 启动，Codex 可以为每次更改启动并驱动一个实例
- Chrome DevTools 协议接入智能体运行时，创建了用于处理 DOM 快照、屏幕截图和导航的技能
- 日志、指标和追踪记录通过本地可观测性堆栈展示给 Codex（LogQL + PromQL）

常见单次 Codex 运行在单个任务上持续超过六个小时（通常在人类睡眠时间）。

### 代码仓库即记录系统

情境管理是使智能体在大型复杂任务中有效发挥作用的最大挑战之一。

**一个大型 AGENTS.md 的失败教训**：
1. 情境是稀缺资源——大型指令文件挤掉任务、代码和相关文档
2. 过多的指导反而无效——当一切都"重要"时，一切都不重要了
3. 立即腐烂——庞杂手册变成陈旧规则的坟场
4. 难以核实——不适合进行机械检查（覆盖率、新鲜度、所有权）

**解决方案**：AGENTS.md 视为内容目录（约 100 行），代码仓库知识库位于结构化的 docs/ 目录中。

```
AGENTS.md
ARCHITECTURE.md
docs/
├── design-docs/
│   ├── index.md
│   ├── core-beliefs.md
├── exec-plans/
│   ├── active/
│   ├── completed/
│   └── tech-debt-tracker.md
├── generated/
│   └── db-schema.md
├── product-specs/
├── references/
├── DESIGN.md
├── FRONTEND.md
├── PLANS.md
├── PRODUCT_SENSE.md
├── QUALITY_SCORE.md
├── RELIABILITY.md
└── SECURITY.md
```

渐进式披露：智能体从小而稳定的切入点开始，被指导下一步该去哪里查看，而不是一开始就被淹没。

专职 linter 和 CI 作业验证知识库的更新状况、交叉链接和结构。一个定期运行的"doc-gardening"智能体扫描过时文档并发起修复 PR。

### 目标是智能体的可读性

从智能体的角度看，它在运行时无法在情境中访问的任何内容都是不存在的。存储在 Google Docs、聊天记录或人们头脑中的知识都无法被系统访问。

团队需要将越来越多的情境推送到仓库中——那次让团队在架构模式上达成一致的 Slack 讨论？如果智能体无法发现它，它就会像迟了三个月入职的新员工一样，对其一无所知。

倾向于选择那些可以完全内化于在仓库中进行推理的依赖项和抽象。"枯燥"的技术由于其可组合性、API 稳定性和在训练集里的表现，往往更容易建立模型。

### 规范架构与品味

仅靠文档本身，没法保持完全由智能体生成的代码库的连贯性。

通过强制执行不变量，而非对实施过程进行微观管理，令智能体能够快速交付而不削弱基础：
- 要求在边界处解析数据形状，但不规定具体实现方式
- 每个业务域划分为固定层（Types → Config → Repo → Service → Runtime → UI）
- 依赖方向经过严格验证，通过自定义 linter 和结构测试机械地强制执行

这种架构通常要等到拥有数百名工程师时才会推迟。对于编码智能体来说，这是早期的先决条件：有了约束，速度才不会下降，架构才不会漂移。

Lint 错误信息会在智能体情境中注入修复指令。

### 吞吐量改变了合并的理念

代码仓库在运行过程中尽量减少阻塞合并门。Pull Request 的生命周期很短。测试偶发失败通常通过后续重跑来解决，而不是无限期地阻碍进展。

在智能体吞吐量远超人类注意力的系统中，纠错成本低，而等待成本高。

### "智能体生成"实际上意味着什么

智能体的产出包括：产品代码与测试、CI 配置和发布工具、内部开发者工具、文档和设计历史、评估框架、审阅评论和回复、管理代码仓库本身的脚本、生产仪表板定义文件。

人类始终参与其中，但工作的抽象层次与过去不同：优先处理工作，将用户反馈转化为验收标准，并对结果进行验证。当智能体遇到困难时，识别缺失的内容——工具、指导与约束、文档——并将其反馈到代码仓库中，始终由 Codex 自己编写修复。

### 不断提高的自主水平

给定一个提示，智能体现在可以端到端驱动一个新功能：
1. 验证代码库的当前状态
2. 重现已报告的漏洞
3. 录制一个演示故障的视频
4. 实施修复措施
5. 通过运行应用程序来验证修复
6. 录制第二个视频，演示解决方案
7. 打开 Pull Request
8. 回应智能体和人类反馈
9. 检测并修复构建故障
10. 仅在需要判断时才交由人工处理
11. 合并更改

### 熵与垃圾收集

Codex 会复现代码仓库中已存在的模式——甚至包括那些不均衡或不够理想的模式。随着时间的推移，这不可避免地导致漂移。

最初团队每周五（占一周的20%）花时间清理"AI 残渣"。不具备可扩展性。

解决方案：将"黄金原则"直接编码到代码仓库中，建立循环清理流程：
1. 共享实用程序包优于手工编写的辅助工具
2. 不使用"YOLO 式"探测数据——验证边界或依赖类型化 SDK
3. 定期运行后台 Codex 任务，扫描偏差、更新质量等级，发起有针对性的重构 PR

功能类似于垃圾回收——技术债务像高息贷款，不断小额偿还优于积累后痛苦解决。

### 我们仍在学习的内容

- 完全由智能体生成的系统中，架构连贯性如何随时间演变
- 人类的判断力在哪些方面能发挥最大作用，以及如何编码这种判断力
- 随着模型功能不断增强，这一系统将如何演变

显而易见的是：构建软件仍然需要纪律，但纪律更多地体现在支撑结构上，而不是代码上。保持代码库一致性的工具、抽象和反馈回路变得越发重要。

当前最棘手的挑战集中在设计环境、反馈回路和控制系统方面，帮助智能体实现目标：大规模构建和维护复杂、可靠的软件。
